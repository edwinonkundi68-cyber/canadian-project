const ImpactCard = ({ title, description }) => {
    return (
        <div className="impact-card">   
            <h3>{title}</h3>
            <p>{description}</p>
        </div>
    )
}   
export default ImpactCard   